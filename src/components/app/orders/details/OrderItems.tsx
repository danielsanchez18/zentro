import { Package } from "lucide-react";
import type { CustomerOrder } from "@/lib/mock/orders";
import { formatOrderMoney } from "@/lib/mock/orders";

export function OrderItems({ order }: { order: CustomerOrder }) { return <section className="overflow-hidden rounded-xl border border-border bg-card"><header className="border-b border-border px-5 py-3"><h2 className="text-sm font-medium">Productos del pedido</h2><p className="mt-1 text-xs text-muted-foreground">Precios y descuentos conservados al momento de la venta.</p></header><div className="divide-y divide-border px-5">{order.lines.map((line) => <article key={line.id} className="flex items-start gap-4 py-4"><span className="flex size-12 shrink-0 items-center justify-center rounded-lg bg-accent text-primary"><Package className="size-5" /></span><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><p className="text-sm font-medium">{line.name}</p><p className="mt-1 text-xs text-muted-foreground">{line.quantity} × {formatOrderMoney(line.unitPrice)}</p></div><p className="text-sm font-semibold tabular-nums">{formatOrderMoney(line.total)}</p></div>{line.notes && <p className="mt-2 rounded-lg bg-muted/40 px-3 py-2 text-xs text-muted-foreground">Nota: {line.notes}</p>}{line.discount > 0 && <p className="mt-2 text-xs text-emerald-600">Descuento: -{formatOrderMoney(line.discount)}</p>}</div></article>)}</div></section>; }
